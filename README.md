# RAVE — Research And Verify Engine

A self-hostable deep-research engine: schema-enforced agents that plan,
search the live web in parallel, cross-check facts deterministically, run an
adversarial verification loop, and emit a fully cited report.

Full documentation lands in the final build phase; see `PLAN.md` for the spec.
